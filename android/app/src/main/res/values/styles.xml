<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark, chrome-free shell: the canvas paints the whole screen. -->
    <style name="AlphaGravityTheme" parent="@android:style/Theme.Material.NoActionBar.Fullscreen">
        <item name="android:windowBackground">@color/ag_background</item>
        <item name="android:colorBackground">@color/ag_background</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
